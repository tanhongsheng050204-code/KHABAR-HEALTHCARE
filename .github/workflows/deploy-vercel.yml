name: Deploy Khabar web to Vercel

on:
  push:
    branches:
      - main

concurrency:
  group: khabar-vercel-production
  cancel-in-progress: true

jobs:
  deploy:
    name: Build and deploy production
    runs-on: ubuntu-latest
    permissions:
      contents: read
    env:
      VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
      VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
      VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Install Vercel CLI
        run: npm install --global vercel@59.25.0

      - name: Pull production configuration
        run: vercel pull --yes --environment=production

      # Remote builds respect the project's space-containing root directory.
      # Vercel's prebuilt uploader currently turns that directory into an
      # invalid serverless-function name.
      - name: Build and deploy production application
        run: vercel deploy --prod --yes
