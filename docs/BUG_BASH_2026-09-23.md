# Bug bash: local run, 23 September 2026

Fictional data only. This follows [`DEMO_RUN_CHECKLIST.md`](DEMO_RUN_CHECKLIST.md) and records what
happened, including what was not run.

## Result

- **Date/time:** 23 Sep 2026, 16:55–17:30 MYT
- **Commits:** before fixes `46ed30d`; after fixes `258b3b9`
- **Environment:** local. API `local` profile (in-memory database, demo clinic), agents service without
  a Gemini key (scripted intake, word-list triage), Next.js dev server on port 3000. No Neo4j, no
  WhatsApp (messages went to the outbox), no Supabase (demo tokens).
- **Roles:** doctor, patient, caregiver (demo accounts); fake patient: Aminah (demo patient)
- **Core story:** PASS after fixes
- **Role/access checks:** PASS
- **Failure/fallback checks:** PASS after fixes (agents service down); Neo4j-down and network-throttle checks NOT RUN
- **Not covered:** the deployed site (behind the code; needs a deploy), real Supabase sign-in, real
  WhatsApp, real Gemini (packet photos used a stand-in, see below)

## What was run

| Step | Outcome |
|---|---|
| Doctor home: call list, counts, clinic directory, approved answers | Works |
| Patient record: pre-visit report, medicines, readings, device link | Works; the medicine check was missing (fixed) |
| Visit: notes → draft → safety check → blocked finalise → written reason → finalise | Works; BM summary reached the outbox |
| Patient: new summary visible, booking a slot, guided intake (4 questions, BM) | Works; intake reached the doctor's pre-visit report with both conditions and the new herb |
| Patient: follow-up reply → doctor's call list | Works; the app showed an empty message (fixed) |
| Caregiver: read-only shared view | Works |
| Patient removes caregiver → caregiver refused | Works: the API refuses record, medicines, readings and summary with 403 at once |
| Packet photo → review → add → pre-visit duplicate finding | Works through the real API and agents, with a local stand-in for Gemini |
| Phone width (390 px): landing, sign-in, doctor home, patient home, visit | No page scrolls sideways; three layout faults fixed |
| Reduced motion on the landing page | Motion off and the toggle says so; hydration error fixed |
| Agents service down | Drafting refuses clearly with nothing lost; replies still reach a person; the medicine check says it is unavailable |

## Defects found and fixed

| # | Defect | Severity | Fix |
|---|---|---|---|
| 1 | With triage down, a patient writing "sakit dada" got only "the clinic will check your message", with no emergency advice | **Safety** | The patient now gets the same 999 advice a red flag gets, in their language, whenever triage could not run |
| 2 | The doctor's patient record did not show the medicine check (duplicate metformin across clinics, herb clash) even though the API returned it | High | Shown under "Medicines & remedies", with a note when the check is unavailable |
| 3 | After a follow-up reply the patient saw an empty message box | High | The API returns the words the patient was sent; the app shows them |
| 4 | A reopened visit showed an empty notes box above a draft made from notes | Medium | The API returns the doctor's notes; the screen loads them |
| 5 | Call-list urgency shown only by colour (every badge said "reply") | Medium | Badges read "urgent · reply", "watch · reply", "review · reply" |
| 6 | "Good afternoon, Dr." (name cut); initials counted bin/binti/anak (Aminah binti Yusof → AB) | Low | Titles kept with the name; initials skip name connectors (AY) |
| 7 | Revoked caregiver still saw a "Consent active" badge | Low | The badge shows only while linked |
| 8 | Patient home said "Complete the intake" right after the intake was done | Low | Says "Answers sent" and offers "Update my answers" |
| 9 | On phones: patient table needed sideways scrolling to reach "Open"; override reason box squeezed to a narrow column; status pill text ran together | Low | Table drops two columns on phones; two CSS specificity conflicts fixed |
| 10 | "1 critical finding still need a written reason" | Low | Singular and plural wording |
| 11 | Landing page hydration error when the device asks for reduced motion | Low | The setting is read after hydration |

## Known, not fixed

- The access log shows each caregiver view twice in the dev server: React runs page effects twice in
  development. Expected not to happen in a production build; not yet confirmed there.
- `next-themes` injects a `<script>` inside a component, which React 19 reports in development only.
- A 404 is logged in the browser console when a patient has no booking or no summary yet; the screens
  handle it.
- The doctor home's "Appointments" card shows "—" after a booking for tomorrow; it counts today only,
  which is correct but may confuse in a demo.
