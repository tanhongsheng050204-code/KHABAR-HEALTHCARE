# Khabar web

The unified Next.js frontend for Khabar.

```powershell
npm install
npm run dev -- -p 3001
```

Copy `.env.example` to `.env.local` when using Supabase email OTP. The local doctor and patient demos use the API at `http://localhost:8080`.
